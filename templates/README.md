# Templates

This directory contains reusable project, document, configuration, or workflow
templates for people and agents.

A template is not a skill by itself. When a template requires a repeatable
agent procedure, keep the template here and place the independently useful
procedure under `skills/`.
